#+title: LLM-LOG-EVIDENCE-008 — Expert CI authority reconciliation GREEN
#+startup: overview

* Scope
- Parent issue: https://github.com/lost-rob0t/llm-log/issues/10
- Design: =research/LLM-LOG-RESEARCH-024-expert-ci-authority-boundary.org=
- PR: https://github.com/lost-rob0t/llm-log/pull/24
- Pre-reconciliation head: =47130371de0a34c7e82c6305a54527a3120ee5d3=
- Reconciled implementation head: =32fc13e304179456919c231b0235c9f6ca1bef65=
- Hosted run: https://github.com/lost-rob0t/llm-log/actions/runs/33306021461

This file is execution evidence only. It does not redefine transport ownership.

* Pre-reconciliation evidence
Hosted run =33304010749= on =47130371de0a34c7e82c6305a54527a3120ee5d3= completed with:

- =common-lisp-expert-integration-contract=: GREEN.
- historical =common-lisp-transport-contract=: RED.

The failing job exercised abandoned Common-Lisp proxy/recorder acceptance work and therefore enforced a runtime ownership model that conflicts with the current explicit architecture. It was a CI-authority mismatch, not a failure of the bounded expert substrate.

* Mutation
Commit =32fc13e304179456919c231b0235c9f6ca1bef65= changes only the branch workflow:

- retains the unchanged =common-lisp-expert-integration-contract=;
- removes the historical Common-Lisp transport and Sento-recorder jobs from this expert branch gate;
- changes no Python capture/transport production code;
- changes no Common Lisp expert production code;
- changes no SWI-Prolog rules;
- changes no Tek9 code;
- changes no dotfiles consumer configuration.

* GREEN
Hosted run =33306021461= executed exact head =32fc13e304179456919c231b0235c9f6ca1bef65=.

Job =common-lisp-expert-integration-contract= completed GREEN. The unchanged Nix contract therefore still proves:

- Common Lisp opens the Tek9-backed expert host;
- one stable event is materialized and retrieved through the bounded Tek9 path;
- KB revision is durable and returned with the typed result;
- the persistent SWI-Prolog worker derives declared =event_transport=;
- rule provenance is present;
- a second query reuses the same worker session;
- undeclared Prolog operations are rejected before invocation.

* Boundary confirmation
- Python remains capture/transport owner.
- Raw capture remains immutable evidence.
- Common Lisp remains expert-runtime owner.
- Tek9 remains the canonical durable expert KB.
- SWI-Prolog remains facts/rules/inference authority.
- No Tek9 primitive change was required.
- Dotfiles remains unchanged and continues to use =~/Documents/AI/proxy=.

* Result
GREEN for the CI/authority reconciliation and the existing bounded #10 Tek9/SWI-Prolog integration contract.

This does not claim #10 complete. Remaining expert-runtime invariants include contradictory replay/idempotency execution, persisted derived-assertion provenance/supersession, reasoner failure/recovery execution, and Python-to-expert fail-open versus explicit fail-closed integration.
