#+title: LLM-LOG-EVIDENCE-001 — HTTP fidelity RED
#+startup: overview

* Scope
- Parent design: ../research/LLM-LOG-RESEARCH-015-http-fidelity-black-box-red.org
- Issue: https://github.com/lost-rob0t/llm-log/issues/10
- PR: https://github.com/lost-rob0t/llm-log/pull/18

* Immutable baseline
- Commit: =0359d92e4e637d888079ddf55e72d2c9124de81a=
- Hosted run: https://github.com/lost-rob0t/llm-log/actions/runs/33282707089
- Check: =Common Lisp transport RED / common-lisp-transport-red=
- Result: expected-RED verifier =success=

* Exact gate
#+begin_src sh
nix build -L .#checks.x86_64-linux.common-lisp-transport-contract
#+end_src

The Nix derivation itself is required to fail on the pre-implementation runtime. The hosted verifier succeeds only when the expected Common Lisp proxy-surface assertions are present in that failure output.

* Observed interpretation
Nix installation completed and the Common Lisp contract reached Rove. The unchanged runtime is behaviorally RED because the declared Common Lisp HTTP proxy surface is not implemented yet.

The contract committed at this baseline additionally contains the raw HTTP fidelity body. As soon as the required surface exists, the same suite proceeds to a loopback raw-upstream/client test for:

- provider selector =/fixture/= removed exactly once before upstream forwarding;
- =POST= method preservation;
- repeated query key and percent-encoded slash preservation;
- binary request body including NUL preserved exactly;
- end-to-end =X-Fidelity= request header preserved;
- upstream =418= status preserved;
- binary response body including NUL preserved exactly;
- two distinct ordered =Set-Cookie= response fields preserved.

No Python runner is used by this authoritative transport contract.

* Boundary
This evidence does not claim HTTP GREEN. It authorizes only the next bounded production slice: implement minimum Common Lisp ordinary-HTTP forwarding against the exact committed contract, then rerun it unchanged.

No Tek9 mutation and no dotfiles mutation occurred in this RED slice.
